export function App() {
  return (
    <main className="app-shell">
      <section className="boot-card" aria-labelledby="boot-title">
        <p className="eyebrow">MARKET ERA</p>
        <h1 id="boot-title">Market terminal initializing</h1>
        <p>The first playable market view is connecting to the authoritative simulation.</p>
      </section>
    </main>
  );
}

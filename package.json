{
  "name": "market-era",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test tests/*.test.mjs",
    "demo": "tsc -p tsconfig.json && node tools/demo.mjs"
  },
  "dependencies": {
    "fastify": "^5.12.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
